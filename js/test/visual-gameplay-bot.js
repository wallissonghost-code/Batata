const EXPECTED_FORWARD=[0,1,2,3];const EXPECTED_BACKWARD=[3,2,1,0];
export class VisualGameplayBot{
constructor(game){this.game=game;this.samples=[];this.cycles=0;this.lastCycle=null;this.lastFrame=null;this.turnFrontFrames=[];this.turnBackFrames=[];this.sequenceFailures=[];this.finishFailures=[];this.seenFinishers=new Set()}
pushUnique(target,frame){if(frame<0)return;if(target[target.length-1]!==frame)target.push(frame)}
validateTransition(sequence,expected,name){if(!sequence.length)return;const compact=sequence.filter((frame,index)=>index===0||frame!==sequence[index-1]),duplicate=new Set(compact).size!==compact.length,exact=compact.length===expected.length&&compact.every((frame,index)=>frame===expected[index]);if(duplicate||!exact)this.sequenceFailures.push({transition:name,sequence:[...compact],expected:[...expected],duplicate})}
auditFinishers(){const g=this.game;if(typeof g.finishTargetY!=='function')return;const target=g.finishTargetY();for(const p of g.finishers){if(this.seenFinishers.has(p.id))continue;this.seenFinishers.add(p.id);const y=p.finishCrossY??p.y;if(y>target)this.finishFailures.push({user:p.user,y,target,reason:'finished-before-crossing-target'})}}
sample(){const g=this.game;if(!g.running)return;const frame=g.dollFrameIndex(),maxFrame=Math.max(0,g.dollFrames.length-1);let frameOk=true;if(g.cycle==='walk')frameOk=frame===0;else if(g.cycle==='watch')frameOk=frame===3&&maxFrame===3;else if(g.cycle==='turn-front'){frameOk=frame>=0&&frame<=3;this.pushUnique(this.turnFrontFrames,frame)}else if(g.cycle==='turn-back'){frameOk=frame>=0&&frame<=3;this.pushUnique(this.turnBackFrames,frame)}if(this.lastCycle!==g.cycle){if(this.lastCycle==='turn-front'){this.validateTransition(this.turnFrontFrames,EXPECTED_FORWARD,'turn-front');this.turnFrontFrames=[]}if(this.lastCycle==='turn-back'){this.validateTransition(this.turnBackFrames,EXPECTED_BACKWARD,'turn-back');this.turnBackFrames=[]}if(this.lastCycle&&g.cycle==='walk')this.cycles++}if(this.lastFrame!==null){if(g.cycle==='turn-front'&&frame<this.lastFrame)frameOk=false;if(g.cycle==='turn-back'&&frame>this.lastFrame)frameOk=false}this.auditFinishers();this.lastCycle=g.cycle;this.lastFrame=frame;this.samples.push({cycle:g.cycle,frame,frameOk,alive:g.players.filter(p=>p.alive&&!p.done).length});if(this.samples.length>600)this.samples.shift()}
report(){const frameFailures=this.samples.filter(s=>!s.frameOk);return{ok:frameFailures.length===0&&this.sequenceFailures.length===0&&this.finishFailures.length===0,expected:{walk:'Total costa',watch:'Frente total',forward:['Total costa','comecando a virar 187KB','Olhando de canto de rosto','Frente total'],backward:['Frente total','Olhando de canto de rosto','comecando a virar 187KB','Total costa']},cycles:this.cycles,samples:this.samples.length,frameFailures:frameFailures.length,sequenceFailures:this.sequenceFailures.slice(-10),finishFailures:this.finishFailures.slice(-10),alive:this.game.players.filter(p=>p.alive&&!p.done).length,eliminated:this.game.players.filter(p=>!p.alive).length,finished:this.game.finishers.length}}
}
